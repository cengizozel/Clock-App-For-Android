<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    tools:ignore="HardcodedText, TextViewEdits, SmallSp" >

    <TextClock
        android:id="@+id/text_clock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:format12Hour="hh:mm:ss a"
        android:textSize="60sp"
        android:textColor="@android:color/black" />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="10dp" >

        <Button
            android:id="@+id/button_world_clock"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:inputType="textCapSentences"
            android:text="World Clock" />

        <Button
            android:id="@+id/button_alarm"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@+id/button_world_clock"
            android:layout_marginStart="5dp"
            android:textSize="12sp"
            android:inputType="textCapSentences"
            android:text="Alarm" />

        <Button
            android:id="@+id/button_stopwatch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@+id/button_alarm"
            android:layout_marginStart="5dp"
            android:textSize="12sp"
            android:inputType="textCapSentences"
            android:text="Stopwatch" />

        <Button
            android:id="@+id/button_timer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@+id/button_stopwatch"
            android:layout_marginStart="5dp"
            android:textSize="12sp"
            android:inputType="textCapSentences"
            android:text="Timer" />

    </RelativeLayout>

</RelativeLayout>